export function getCategoryMetadata(categoryLabel: string) {
  const title = `${categoryLabel} Blog - ScriptingPixels`;
  const description = `Explore insights, tips, and tutorials about ${categoryLabel} on ScriptingPixels — your resource for building better websites with design systems, modern tools, and creative workflows.`;

  return { title, description };
}
